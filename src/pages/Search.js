// import React from "react";
// import { AppProvider } from "../context";

// const newSearch = () =>{
//     const { newSearch ,search} = 
//         const newChampionSearch = championList.filter((item) => item.includes(searchTerm))
//         setChampions(newChampionList);
//         setLoading(false);
//       } catch (error) {
//         setLoading(false);
//       }

// };

// export default search;